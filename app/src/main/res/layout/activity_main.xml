<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingLeft="20dp"
    android:paddingRight="20dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingTop="10dp">

        <Button
            android:id="@+id/btn_go_campaign"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="@drawable/border_gray_b3e3e5e9_bg_transparent_radius_1dp"
            android:foreground="@drawable/common_click_effect"
            android:text="맞춤쿠폰 보러가기"
            android:textColor="#222222"
            android:textSize="20dp" />

        <TextView
            android:id="@+id/txt_badge_type"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_marginTop="-10dp"
            android:background="#fa2828"
            android:includeFontPadding="false"
            android:paddingLeft="5dp"
            android:paddingRight="5dp"
            android:textColor="#ffffff"
            android:textSize="15dp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="HOT"
            tools:visibility="visible" />

    </RelativeLayout>

    <Button
        android:id="@+id/btn_terms_agreement_process"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="30dp"
        android:background="@drawable/border_gray_b3e3e5e9_bg_transparent_radius_1dp"
        android:foreground="@drawable/common_click_effect"
        android:text="약관 동의 Flow"
        android:textAllCaps="false"
        android:textColor="#222222"
        android:textSize="20dp" />

</LinearLayout>